This is a simple project to use the convertMapPoints commonJS module to reference the location of an annotation and then take action.  In this case we are adding a custom view pop-up instead of the standard annotation.

Video Demo here: http://screencast.com/t/FadjZ4HTAC15
